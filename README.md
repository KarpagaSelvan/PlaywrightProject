## Environment Configuration

This project supports multiple environments (qa, prod).

Actual config files and auth state are not committed for security reasons.
Sample config templates are provided under `/config/*.example`.

Auth state is generated dynamically using AuthSetup.
